package com.moviebrowser.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Defines the application's color palette.
 *
 * Contains:
 * - Brand colors (Primary, Accent)
 * - Light theme colors
 * - Dark theme colors
 * - Corresponding "on" colors used for text and icons
 *
 * Used by the app's theme system to ensure
 * consistent styling across the UI.
 */

val Primary = Color(0xFF1F3A5F)
val OnPrimary = Color(0xFFFFFFFF)

val Accent = Color(0xFFF4C56A)
val OnAccent = Color(0xFF1A1A1A)

val BackgroundDark = Color(0xFF0E1117)
val SurfaceDark = Color(0xFF161B22)

val OnBackgroundDark = Color(0xFFE6EAF2)
val OnSurfaceDark = Color(0xFFE6EAF2)

val BackgroundLight = Color(0xFFF6F8FC)
val SurfaceLight = Color(0xFFFFFFFF)

val OnBackgroundLight = Color(0xFF1A1F2B)
val OnSurfaceLight = Color(0xFF1A1F2B)





